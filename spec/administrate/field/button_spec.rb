# frozen_string_literal: true

RSpec.describe Administrate::Field::Button do
  it "has a version number" do
    expect(Administrate::Field::Button::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
